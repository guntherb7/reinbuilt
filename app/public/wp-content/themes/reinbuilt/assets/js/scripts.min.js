const navLinks=document.querySelectorAll(".nav-link");for(const a of navLinks)a.href===window.location.href&&a.classList.add("active");const hamburgerMenu=document.getElementById("hamburger-menu"),bottomNav=document.getElementById("bottom-nav");hamburgerMenu.addEventListener("click",()=>{bottomNav.classList.toggle("open")});